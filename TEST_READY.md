# TEST_READY.md — E2E 1000回自動検証スクリプト準備完了

要件 R4 に対応する E2E 1,000回連続問題生成自動検証テストスクリプト `verify_1000_runs.js` の作成および動作確認が完了しました。

---

## 1. テスト実行コマンド

### プロジェクトルートから実行
```bash
node verify_1000_runs.js
```
または特定の試行回数を指定して確認する場合:
```bash
node verify_1000_runs.js --runs 10
```

### `abacus_app` ディレクトリから実行
```bash
cd abacus_app
npm run verify:1000
```
または `npx vite-node verify_1000_runs.js`

---

## 2. 検証スクリプト `verify_1000_runs.js` の仕様

| 要件カテゴリ | 検証内容と厳密性 |
| :--- | :--- |
| **実行モデル (R4)** | `generateDivisionProblems()` を **1,000回連続呼び出し**（合計 **10,000問** の除算問題）し、全試行をチェック。 |
| **数学的整合性 (R1)** | ① 被除数（Dividend）が正の整数（`Number.isInteger(div)` かつ `div > 0`）であること。<br>② 整数商・確信商（`up`）・還元商（`down`）および小数の除数において四捨五入計算の整合性（`Math.round(Dividend / B) === Answer`）が厳密に保持されていること。<br>③ `null` や `undefined` の返却およびフォールバック（`console.warn` 発行、先頭桁網羅不足）が発生していないこと。 |
| **出現回数完全均等配分 (R2)** | 10問1セット全体の全桁（除数55桁 ＋ 商55桁 ＝ **合計110桁**）において、`0`〜`9` の各数字が**正確に各11回ずつ**出現していることを検証（1桁の偏差も許容しない）。 |
| **既存品質制約 (R3)** | ① **末尾数字網羅**: 10問の商末尾桁および除数末尾桁に `0`〜`9` がそれぞれ重複なく1回ずつ出現すること。<br>② **先頭数字制限/網羅**: 商・除数の先頭桁に `0` が入らず（`!= 0`）、被除数（Dividend）の先頭桁に `1`〜`9` の全数字が出現すること。<br>③ **AA/ABA誤発生防止**: 対象指定された1行以外の残り8行に不必要な連続桁（AA）や挟み桁（ABA）が発生していないこと。 |
| **集計・判定と終了コード** | 成功数、フォールバック数、R1数学的整合性エラー数、R2桁数配分ズレ数、R3品質制約違反数を集計出力。<br>全1,000回がパスした場合は **終了コード `0`**、違反が1件でも検出された場合は **非0 (終了コード `1`)** で終了。 |

---

## 3. 現状コードに対するベースライン検出検証結果

現行の `generateDivisionProblems()` に対して検証スクリプトを実行し、以下の違反が厳密に検出されることを確認しました：

1. **R2 桁数配分ズレ**: 10問の合計桁数が 111 桁（110桁オーバー）となり、数字 `0` が 12 回出現する問題パターンを検出。
2. **R3 先頭数字制限違反**: 小数除数において除数の先頭桁に `0`（`0.xxx`）が出現するケースを検出。
3. **R1/R2/R3 フォールバック空データ検出**: 問題生成失敗時に初期化済みの `null` データ群が返却され、全計算・桁数が不正になる現象を正確に検出。

---

## 4. Tier 要約

- **Tier 1 (Core Engine Verification)**: 単一・少数回テストでは見落とされがちな確率的フォールバックや特定の桁数割り当てエッジケースを1,000回連続試行で全数網羅。
- **Tier 2 (Integrity Guard)**: 数学的計算整合性、桁数配分バランス、品質制約の3軸で妥協のない検証を実施。
- **Tier 3 (CI / Automation Ready)**: 標準終了コード（0 / 1）と構造化ログにより、CI/CD環境や次段階の実装改善における回帰テストとして即座に使用可能。
