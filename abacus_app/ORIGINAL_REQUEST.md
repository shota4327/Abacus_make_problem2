# Original User Request

## 2026-08-02T03:12:55Z

# Teamwork Project Prompt — Draft

除算問題ジェネレーターにおいて、1未満の小数の問題（除数が0.xxx）を作成する際、頭に付く0の数（`zc`）が「1個」「2個」「3個」の3パターンで、長期的（確率的）に完全に均等に出現するようにアルゴリズムを改良する。

Working directory: c:\Users\kneko\OneDrive\60  ツール\Git\Abacus_make_problem2\abacus_app

Integrity mode: development

## Requirements

### R1. 0の数の均等出現
1未満の小数の問題において、頭につく0の数が 1個（`0.x...`）、2個（`0.0x...`）、3個（`0.00x...`）のいずれかになり、それぞれが約33%の確率で均等に出現するようにすること。

### R2. 枠（7桁）制約への対応
解答用紙の枠は7桁です。0を3個つけるためには、有効数字が4桁（7-3=4）以下の行である必要があります。0を均等に出現させるため、対象となる行（`candRow`）を選ぶ際に、必要な `zc` に合わせて「短い桁数の行」を優先・抽出して割り当てるロジックを実装してください。

### R3. 既存の堅牢なアルゴリズムの維持
前回の改修で実装された「誤差周辺探索（`getExactDividendForDecimal`）」等のロジックを絶対に維持し、以下の品質を落とさないこと：
- 0〜9の数字の出現回数が、全体で完全に目標値と一致すること（ズレ0）。
- 無限ループやマイナス値への突入、生成のフォールバックが発生しないこと。
- 末尾数字や先頭数字の網羅などの既存制約が守られること。

### R4. 自動検証の実施
1000回以上の連続生成テストを行う自動検証スクリプトを更新（または新規作成）し、R1の「0の数の出現分布が1個・2個・3個でほぼ均等（約33%ずつ）になっているか」をカウントして証明してください。もちろん、R3の既存制約エラーも0であることを証明します。

## Acceptance Criteria

### 制約充足と品質の証明
- [ ] エージェントチームが自律的に行選択のアルゴリズムを決定・実装していること。
- [ ] 1未満の小数の0の数が、1〜3の範囲でランダムかつ均等に出現すること。
- [ ] 出現回数（0〜9）の完全一致、フォールバックなし、マイナス値なしなど、前回の成果がすべて保たれていること。

### 自動検証の合格
- [ ] 自動検証テストスクリプトが用意されていること。
- [ ] 1000回連続テストを実行し、「フォールバック0」「制約エラー0」であることに加え、「0の数が1個: ◯回, 2個: ◯回, 3個: ◯回」の分布がほぼ 1:1:1 の割合で出力ログに提示されていること。
